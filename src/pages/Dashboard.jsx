import PageShell from '../components/PageShell.jsx';

function Dashboard() {
  return (
    <PageShell pageName="Dashboard" owner="Zainab">
      <div className="starter-note">
        <h3>Zainab: build the Dashboard page here</h3>
        <p>This file is intentionally a starter shell. Use the wireframe and development guide to build this page's UI.</p>
        <ul>
          <li>Create the dashboard overview cards, quick actions, task table, and activity feed.</li>
        </ul>
      </div>
    </PageShell>
  );
}

export default Dashboard;
